import 'package:flutter/material.dart';

class AppColors {
  static const Color primaryColor = Color(0xFF6200EA); // Ana renk
  static const Color secondaryColor = Color(0xFF03DAC6); // İkincil renk
  static const Color backgroundColor = Color(0xFFF6F6F6); // Arka plan rengi
  static const Color textColor = Color(0xFF333333); // Metin rengi
  static const Color accentColor = Color(0xFFFFC107); // Vurgu rengi
  static const Color likeColor = Color(0xFFFF0000); // Beğenme rengi (kırmızı)
  static const Color secondaryTextColor =
      Color(0xFF757575); // Yorumlar için gri ton

  static const Color buttonColor = Color(0xFF007AFF); // Buton rengi
  static const Color borderColor = Color(0xFFDDDDDD); // Sınır rengi
  static const Color shadowColor = Color(0x29000000); // Hafif gölge

  // Ekstra renkler (eğer gerekiyorsa)
  static const Color errorColor = Color(0xFFB00020);
  static const Color successColor = Color(0xFF4CAF50);
}
